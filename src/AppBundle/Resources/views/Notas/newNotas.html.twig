{% extends 'base.html.twig' %}

{% block title %}Nueva Nota{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-4 offset-3">
                <h1> Nueva Nota </h1>

                <form>

                    <div class="form-group ">
                        <label for="id">ID</label>
                        <input type="text" class="form-control col-lg-2" id="id" value="{{ ticketID }}" disabled="disabled">
                    </div>

                    <div class="form-group ">
                        <label for="usuarioID">Usuario ID</label>
                        <input type="text" class="form-control col-lg-2" id="usuarioID" value="{{ usuarioID }}" disabled="disabled">
                    </div>

                    <div class="form-group">
                        <label for="descripcion">Nota:</label>
                        <textarea class="form-control" rows="5" id="descripcion"></textarea>
                    </div>


                    <button class="btn btn-success" id="addRol"><span>Guardar</span></button>
                    <a href="{{ path('rolesIndex') }}" class="btn btn-danger"><span>Cancelar</span></a>

                </form>

            </div>
        </div>
    </div>


{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script>

        function addRol(rol){

           // alert(JSON.stringify(rol));

            $.ajax({

                type:"POST",
                data: JSON.stringify(rol),
                url: Routing.generate('addRol'),
                error: function () {

                },
                success: function (data) {
                   // alert(data);

                    window.location = Routing.generate('rolesIndex');
                }
            });
        }

        $(document).ready(function () {

            $('#addRol').click(function () {

                var nombre = $('#nombre').val();
                var departamentoProyecto = $('#departamentoProyecto').val();
                var descripcion = $('#descripcion').val();


                addRol({ nombre : nombre, departamentoProyecto : departamentoProyecto, descripcion : descripcion });
            });
        });

    </script>


{% endblock %}